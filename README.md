## Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
Restaurant# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
Analytics

This# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
repository# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
contains# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
the# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
source-controlled# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
project# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
structure# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
for# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
the# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
restaurant# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
analytics# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
solution# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
in# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
Databricks.

### Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
Included# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
assets

*# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
Lakeflow# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
Spark# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
Declarative# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
Pipelines# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
source# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
files# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
for# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
bronze# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
ingestion,# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
silver# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
transformations,# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
and# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
gold# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
aggregations
*# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
Declarative# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
Automation# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
Bundles# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
resource# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
definitions# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
for# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
pipelines,# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
job# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
orchestration,# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
and# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
dashboard# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
deployment
*# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
Export# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
location# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
for# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
the# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
Restaurant# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
Analytics# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
dashboard# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
JSON
*# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
Environment# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
variable# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
placeholders# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
for# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
dev# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
and# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
prod# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
deployments

### Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
Project# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
layout

*# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
`src/ingestion`# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
-# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
bronze# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
ingestion# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
source# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
files
*# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
`src/silver`# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
-# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
silver# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
transformation# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
source# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
files# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
and# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
SQL
*# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
`src/gold`# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
-# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
gold# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
aggregation# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
source# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
files
*# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
`resources/pipelines`# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
-# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
bundle# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
pipeline# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
definitions
*# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
`resources/jobs`# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
-# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
bundle# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
job# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
definitions
*# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
`resources/dashboards`# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
-# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
bundle# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
dashboard# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
definitions
*# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
`dashboards`# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
-# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
exported# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
dashboard# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
JSON# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
artifacts
*# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
`conf`# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
-# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
environment-specific# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
non-secret# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
variables
*# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
`docs`# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
-# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
architecture# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
and# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
runbook# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
notes

### Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
Next# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
steps

1.# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
Replace# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
placeholder# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
values# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
in# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
`databricks.yml`# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
and# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
`conf/*/variables.yml`.
2.# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
Move# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
the# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
Event# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
Hub# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
connection# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
string# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
to# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
a# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
Databricks# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
secret# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
scope# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
before# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
first# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
deployment.
3.# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
Export# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
the# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
latest# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
dashboard# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
JSON# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
into# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
`dashboards/restaurant_analytics_dashboard.lvdash.json`.
4.# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
Create# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
a# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
Git# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
folder# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
or# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
connect# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
this# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
structure# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
to# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
your# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
remote# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
repository.
5.# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
Validate# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
and# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
deploy# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
with# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
the# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
Databricks# Restaurant Analytics — Azure Databricks

End-to-end streaming analytics platform for a multi-city restaurant chain, built on Azure Event Hubs, Lakeflow Spark Declarative Pipelines, Unity Catalog, and AI/BI Dashboards.

## Architecture

```
Azure Event Hubs
      |
      v
[pipeline_ingestion_eventhub]  Bronze (01_bronze)
      |
      v
[pipeline_tranfm_silver]       Silver (02_silver)
      |
      v
[pipeline_trnsfm_gold]         Gold   (03_gold)
      |
      v
[Restaurant Analytics Dashboard]
```

## Quick Start

```bash
databricks secrets create-scope restaurant-analytics
databricks secrets put-secret restaurant-analytics eh-connection-string
databricks bundle validate
databricks bundle deploy -t dev
databricks bundle run -t dev daily_master_job
```

See `docs/` for architecture, data model, and runbook.
CLI.
